# Verification log — v0.1.0 (2026-09-12)

## Verified

| Check | Where | Result |
|---|---|---|
| Biome lint/format, TypeScript strict, `next typegen` | local + CI quality job | pass |
| Unit tests (13): edge rules ↔ golden `.htaccess`, checkout params (no `off`/`ref`), commerce facts | local + CI | pass |
| Static export build (14 routes), `out/.htaccess`, `build-info.json` | local + CI | pass |
| Media budget/provenance: 169 files, 15.07 MB, 174 manifest outputs | local + CI | pass |
| Playwright — smoke, axe WCAG 2.2 AA (all routes incl. 404), offer modes (`?downsell=1`, `?offer=downsell`, with UTMs; pre-paint attribute at DOMContentLoaded), widget lifecycle (script abort, never-renders timeout, reserved height), commerce (allowlisted params, one CheckoutIntent per click with unique ids, hub passthrough, ViewContent once, no pixel without config), consent (no banner/cookies without config, footer control, legal links), motion (reduced-motion reveals, click-to-play video with controls and text alternative, gallery arrows/dots/zoom), navigation crawl (no dead links, sitemap scope, canonicals, media resolve, LCP dimensions) | local: 153 passed on chromium-1440/768/390 + reduced-motion; CI: pass | pass |
| Lighthouse CI (mobile emulation, 2 runs × 4 URLs): accessibility ≥ 0.95 (error), CLS ≤ 0.1 (error), performance/LCP/TBT (warn) | CI | pass; byte-budget warnings only on `/imprime-y-juega/` (Hotmart widget bundle, third party) |
| Compression of text assets by the local server (brotli/gzip) | local curl | pass |

## Not verified in this release

- WebKit and the 1280/1024/430/360 widths (nightly matrix runs on schedule).
- Real Hotmart purchase → upsell → downsell → thank-you flow with the live funnel widget on the new origin.
- Production deployment and the deployed revision check (needs the `deploy` branch connected in hPanel).
- Field Core Web Vitals (CrUX/Search Console) — available ≥ 28 days after launch.
- Consent banner with a configured pixel ID (`E2E_EXPECT_CONSENT` spec runs against a configured build).
- Hostinger `.htaccess` directives in production (`Header set`, `ErrorDocument`, Range/206) — verify with the matrix in `docs/deployment.md`.

## Known launch gates (blocking the Deploy workflow until resolved)

- `[[PLACEHOLDER]]` seller details in `content/es/legal/seller.ts` (`docs/legal-checklist.md`).
- Four demo clips `pending-owner-confirmation` in `media/manifest.json`.
