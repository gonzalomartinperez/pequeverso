# Verification log — v0.2.0 (2026-09-14)

## Verified

| Check | Where | Result |
|---|---|---|
| Biome lint/format, TypeScript strict, `next typegen`, `knip --production` (no temporary ignores) | local + CI build job | pass |
| Unit tests: edge rules ↔ golden `.htaccess`, checkout params, commerce facts (`US$14,99`), media manifest, product registry (schema, media ids, page sums, env references), tracking (forbidden events, consent v2), content invariants | local + CI | pass |
| Static export (13 pages), bundle budgets per route (`config/budgets.json`), rendered-HTML checks, media budget 223 files / 16.62 MB | local + CI | pass |
| Playwright `PW_SET=pr` (chromium 1440/768/390 + reduced motion): smoke, axe WCAG 2.2 AA, offer modes (SSR counters, per-mode sticky labels), widget lifecycle, commerce (checkout origin, `target=_blank`, allowlisted params, one `CheckoutIntent` per click), consent (notice mode without an integration, full banner with a pixel id in CI), motion, navigation crawl, LCP preload, tracking | local: 196 passed / 27 environment skips (PR #20); CI: pass on #19 and #20 | pass |
| Lighthouse CI (mobile emulation, 2 runs): accessibility ≥ 0.95, CLS ≤ 0.1 | CI on #19 and #20 | pass |
| Footer and social links render as icon-only circles at 390 and 1440 | local screenshots | pass |
| Production revision match, smoke, headers, `PW_SET=prod` after each merge | `post-deploy-verify` workflow | pass through `24ccb75`; `912b3cb` pending at the time of writing |

## Not verified in this release

- WebKit and the 1280/1024/430/360 widths (nightly matrix) and the visual baselines (first
  nightly run with `update_snapshots` generates them on Linux).
- Real Hotmart purchase → upsell → downsell → thank-you with the live widget (owner checklist in
  `docs/hotmart-funnel.md` §5; no card was charged).
- Meta Pixel in production (`NEXT_PUBLIC_META_PIXEL_ID` not loaded in hPanel yet).
- Field Core Web Vitals (CrUX/Search Console).

## Open owner items

- `[[PLACEHOLDER]]` seller details in `content/es/legal/seller.ts`.
- Four demo clips `pending-owner-confirmation` in `media/manifest.json` (deploy strict gate).
- Hostinger environment variables (`docs/deployment.md`), `release` branch for the approval gate.
- Hotmart dashboard URL changes, guarantee days (7 vs 15), author's note (`creator.enabled`).
