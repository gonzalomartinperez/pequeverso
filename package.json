{
  "name": "pequeverso",
  "version": "0.2.0",
  "private": true,
  "description": "Pequeverso commercial website: static Next.js landings for the Grafismo Fonético printable kit, with Hotmart checkout.",
  "homepage": "https://pequeverso.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/gonzalomartinperez/pequeverso.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22.18 <25",
    "npm": ">=10.9 <12"
  },
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/check-env.ts && node scripts/build-info.ts && next build --webpack && node scripts/gen-htaccess.ts",
    "build:standalone": "cross-env NEXT_OUTPUT=standalone npm run build",
    "start": "node scripts/start.ts",
    "typegen": "next typegen",
    "typecheck": "next typegen && tsc --noEmit && tsc -p tsconfig.node.json",
    "typecheck:media": "tsc -p tools/media",
    "lint": "biome ci --error-on-warnings .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "node --env-file-if-exists=.env.example --test \"tests/unit/**/*.test.ts\"",
    "test:e2e": "cross-env PW_SET=pr playwright test",
    "test:e2e:nightly": "cross-env PW_SET=nightly playwright test",
    "check": "npm run lint && npm run typecheck && npm run test && npm run knip:production && npm run build && npm run check:media && npm run check:placeholders && npm run check:bundle && npm run check:scene && npm run check:rendered",
    "check:env": "node scripts/check-env.ts",
    "check:media": "node scripts/check-media-budget.ts",
    "check:placeholders": "node scripts/check-placeholders.ts",
    "check:placeholders:strict": "node scripts/check-placeholders.ts --strict",
    "lhci": "lhci autorun",
    "smoke": "node scripts/smoke.ts",
    "start:static": "node scripts/serve-static.ts",
    "test:e2e:visual": "cross-env PW_SET=visual playwright test",
    "test:e2e:prod": "cross-env PW_SET=prod playwright test",
    "knip": "knip --no-progress",
    "knip:production": "knip --no-progress --production",
    "check:bundle": "node scripts/check-bundle-budget.ts",
    "check:scene": "node scripts/check-scene-budget.ts",
    "analyze": "cross-env ANALYZE=1 next build --webpack",
    "check:rendered": "node scripts/check-rendered.ts"
  },
  "dependencies": {
    "@base-ui/react": "1.8.0",
    "class-variance-authority": "0.7.1",
    "cn": "0.3.0",
    "embla-carousel-react": "8.6.0",
    "gsap": "3.15.0",
    "lucide-react": "1.47.0",
    "next": "16.3.5",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "three": "0.186.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@biomejs/biome": "2.5.14",
    "@lhci/cli": "0.15.1",
    "@next/bundle-analyzer": "16.3.5",
    "@playwright/test": "1.63.0",
    "@tailwindcss/postcss": "4.3.3",
    "@types/node": "24.13.6",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "@types/three": "0.186.0",
    "cross-env": "10.1.0",
    "knip": "6.37.0",
    "postcss": "8.5.28",
    "tailwindcss": "4.3.3",
    "typescript": "7.0.2"
  },
  "type": "module"
}
