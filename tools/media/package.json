{
  "name": "@pequeverso/tools-media",
  "version": "0.1.0",
  "private": true,
  "description": "Build-time media pipeline for pequeverso.com (sharp + ffmpeg). Not part of the site bundle.",
  "type": "module",
  "engines": {
    "node": ">=22.18 <25"
  },
  "scripts": {
    "build": "node media-build.ts",
    "check": "node media-build.ts --check"
  },
  "devDependencies": {
    "@types/ffprobe-static": "2.0.3",
    "@types/node": "24.13.4",
    "ffmpeg-static": "5.3.0",
    "ffprobe-static": "3.1.0",
    "sharp": "0.35.4",
    "svgo": "4.1.0"
  }
}
