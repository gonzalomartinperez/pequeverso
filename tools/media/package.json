{
  "name": "@pequeverso/tools-media",
  "version": "0.1.0",
  "private": true,
  "description": "Build-time media pipeline for pequeverso.com (sharp + ffmpeg). Not part of the site bundle.",
  "type": "module",
  "engines": {
    "node": ">=22.12 <25"
  },
  "scripts": {
    "build": "node media-build.mjs",
    "check": "node media-build.mjs --check"
  },
  "devDependencies": {
    "ffmpeg-static": "5.3.0",
    "ffprobe-static": "3.1.0",
    "sharp": "0.35.4",
    "svgo": "4.1.0"
  }
}
